version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: pemira_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always
    command: redis-server --appendonly yes

volumes:
  redis_data:
